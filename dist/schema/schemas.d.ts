import { NodeName } from './nodes';
import { MarkName } from './marks';
import { Schema } from 'prosemirror-model';
export declare enum RtSchema {
    Default = 0
}
export declare const SchemasMap: {
    0: Schema<NodeName, MarkName>;
};
